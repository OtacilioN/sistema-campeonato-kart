Pill chip for filter rows (months / etapas / Geral) and selectable segments. Active = filled black (default) or red (brand).

```jsx
<Chip>Jan</Chip>
<Chip active>Geral</Chip>
<Chip active tone="brand">Próximos</Chip>
```
